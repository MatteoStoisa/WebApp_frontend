export class Student {
    id: string;
    name: string;
    firstName: string;

    constructor(i: string, n: string, fn: string) {
        this.id = i;
        this.name = n;
        this.firstName = fn;
    }
}
